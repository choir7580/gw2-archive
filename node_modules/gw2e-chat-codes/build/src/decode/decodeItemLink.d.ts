import { ChatCodeStruct } from '../ChatCodeStruct';
export declare function decodeItemLink(struct: ChatCodeStruct): {
    type: "item";
    quantity: number;
    id: number;
    skin: number | undefined;
    upgrades: [number] | [number, number] | undefined;
};
